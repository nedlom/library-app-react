import React, { Component } from "react";

const BooksContainerTest = props => {
  
    console.log(props.match.params.id)
    return (
      <div>Made it here at least</div>
    )
  
}

export default BooksContainerTest